# 超导磁通探针整数相位面复核

从带整周跳变的有向边差观测中，**精确**复原一张可用于补偿的整数相位面。
所有重计算在浏览器内的 Web Worker 中完成，页面本身是零运行时依赖的纯静态应用。

## 解决的问题

- 探针编号 `0 … n-1`（2 ≤ n ≤ 40），其中一个**参考探针固定相位为 0**；
- 每个探针有整数相位范围 `[lo_i, hi_i]`；
- 有向观测边 `u → v` 给出目标差值 `t_e` 与正整数权重 `w_e`；
- 在**所有**范围内整数赋值中最小化

      Σ_e w_e · | (x_v − x_u) − t_e |

- 总成本相同时，取按探针标识升序的相位向量 `(x_0, x_1, …)` **字典序最小**者。

不沿生成树逐边累加（闭环矛盾时局部传播不是最优），不使用浮点，也不枚举全赋值。

## 算法：凸整数标签 → 最小割（一次最大流）

有序整数标签的凸代价可归约为 s-t 最小割（Ishikawa 链式图）：

- 二值变量 `z_{i,k} = [x_i ≥ k]`，节点 `(i,k)` 在源侧 ⇔ `z=1`；
- 链内 `+∞` 弧 `(i,k+1) → (i,k)` 强制截集是一个阈值；
- 成对项 `w·|x_v − x_u − t|` 的受限二阶差分仅当 `l − k = t` 时为 `2w`，
  对应弧 `(u,k) → (v,l)`；有限定义域边界的一阶项用源/汇弧精确补偿；
- 参考探针用 `+∞` 钉死弧固定为 0；
- 整数容量 Dinic（显式栈迭代实现，宽范围不栈溢出）求最大流；
- 最大流后从源点沿残量弧可达的集合是**唯一的最小最小割**，
  它在所有最优割中逐分量最小，因而解码出的正是字典序最小的最优解——无需多轮求解。

总代价与每条边的实际差/残差/贡献在求解后按定义用整数复算，并与网络值交叉核对。

## 最优解归属审计（同一次最小割残量网络 · SCC 偏序）

复核得到相位面后，校准员可**发起最优解归属审计**：查看每个探针在保持当前
最小总代价时能够取到的完整整数相位区间，并区分三种结论——

- **固定**（`fixed`）：区间退化为单点，该探针在一切最优赋值中取值唯一；
- **可变**（`variable`）：区间为 `[min, max]`，存在同成本多相位面；
- **参考点固定**（`reference`）：参考探针被 `+∞` 钉死弧固定为 0 的单点。

审计严格基于**同一次**精确最大流完成后的残量网络，不逐探针重跑求解、
不用浮点近似、也不枚举最优赋值：

1. 取残量容量 > 0 的弧构造残量图（显式栈迭代 Kosaraju，宽范围不栈溢出），
   求强连通分量并缩点，得到最小割格的 SCC 偏序 DAG；
2. 阈值节点 `(i,k)`：S 沿残量弧可达 ⇒ 一切最小割强制源侧（z 恒 1）；
   自身可达 T ⇒ 一切最小割强制汇侧（z 恒 0）；皆否 ⇒ 自由 SCC；
3. 最小最小割（S 残量可达集）给出逐探针 `min` 与规范相位，
   最大最小割（不可达 T 的最大闭包）给出 `max`；阈值链单调性保证区间连续；
4. 取到两端值的见证割（最小/最大最小割）均按定义整数复算代价交叉核对。

结果表保留**规范相位、最小值、最大值、状态**四列；点选探针行列出使其取到
两端值的最小割侧判定（强制源/汇区间与自由翻转窗口）及自由 SCC 的联动成员。

## 浏览器中的并发生命周期

- 最小割运行在 [Web Worker](src/worker/solver.worker.js) 中，异步扫描会周期性让出事件循环；
- 每次复核带一个**代际令牌**；草稿修改、发起新复核或点击取消都会作废旧令牌，
  旧计算在下一个让出点停止且其结果永不回传，主线程也只接受当前代际的消息——
  旧计算不可能覆盖当前界面状态；
- **归属审计复用同一次成功复核保留的残量网络现场**（不重新求解）。草稿编辑、
  取消或新复核会立即清空该现场，Worker 只受理与当前成功复核代际精确匹配的
  `audit` 请求，代际不符一律回 `auditStale` 拒绝；
- 校验失败（范围为空、参考点 0 越界、边端点不存在、权重非正整数等）会
  在页面上定位到具体探针行/边字段，并清除旧结论。

## 本地开发

```bash
npm install
npm test       # node --test：含 400 组随机实例对暴力枚举的对照、
               # 500 组审计区间对全赋值包络的对照、20 万阈值层宽范围、
               # Worker 代际取消与审计代际匹配的端到端测试等
npm run dev    # 开发服务器
npm run build  # 产出纯静态文件 dist/
```

## Docker Compose

`compose.yaml` 提供两个服务：

```bash
# 页面（静态应用），宿主机端口默认 8080，可用 WEB_PORT 覆盖：
docker compose up web
#   浏览器打开 http://localhost:8080
WEB_PORT=9000 docker compose up web

# 一次性复核容器：代码测试 → 构建检查 → HTTP 冒烟，完成后自行退出，
# 容器退出码即复核结果（0 成功，非 0 失败）：
docker compose up --build verify
```

- `web`：多阶段构建，`vite build` 产物由 nginx 提供；
  容器健康检查直接抓取首页并核对应用标记（静态应用可用才健康）。
- `verify`：等待 `web` 健康后运行 [scripts/verify.sh](scripts/verify.sh)——
  `npm test`、`npm run build`（并核对 Worker bundle 已产出）、
  对首页/JS/CSS/Worker 资源/`/healthz` 做 HTTP 冒烟，任一失败即以非零码退出。

## 内置样例

页面载入时默认填充“闭环矛盾三角形”：

| 边 | 目标差 | 权重 |
|---|---|---|
| 0→1 | 1 | 10 |
| 0→2 | 1 | 1 |
| 1→2 | 1 | 10 |

按录入顺序选生成树 `{0→1, 0→2}` 逐边累加得 `(0,1,1)`，高权闭合边 `1→2`
残差 −1，代价 10；精确最小割最优解为 `(0,1,2)`，只有低权弦残差 1，代价 **1**。
另提供一个四环“局部累加失败”样例（按钮载入）。
点击「载入同成本多相位面样例」可载入三条边**等权**的矛盾环：闭环残差恒为 1，
最优面有 `(0,1,1)`、`(0,1,2)`、`(0,2,2)` 三张同成本面，发起归属审计即可
观察到探针 1、2 的可变区间 `[1,2]`，参考探针为单点（高权样例则全部单点）。

## 目录结构

```
src/lib/maxflow.js         整数 Dinic（迭代、可取消、异步让出）
src/lib/solver.js          链式最小割归约、校验、字典序最小最优解、SCC 归属审计
src/worker/solver.worker.js 代际令牌、取消协议与审计代际匹配
src/ui/                    页面录入、渲染与状态一致性
test/                      node:test 单元/对照/Worker 端到端测试
scripts/verify.sh          Compose verify 容器入口
docker/nginx.conf          静态站点 + /healthz
```
